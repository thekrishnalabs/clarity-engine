import { createFileRoute, Link } from "@tanstack/react-router";
import { PageHero, CtaBand } from "@/components/hiren/Section";

export const Route = createFileRoute("/about")({ head: () => ({ meta: [{ title: "About — Hiren Kundli" }, { name: "description", content: "About Hiren Kundli, a structured decision clarity system focused on patterns and direction." }, { property: "og:title", content: "About Hiren Kundli" }, { property: "og:description", content: "A calm system for clarity, patterns, and decisions." }] }), component: AboutPage });
function AboutPage() { return <><PageHero eyebrow="About" title="Built for clarity, not prediction." body="Hiren Kundli helps people observe repeating patterns, understand decision pressure, and choose next steps with a structured frame." /><section className="hk-container grid gap-5 pb-12 md:grid-cols-3">{["No fear-based language", "No fixed outcome claims", "No mystical shortcuts"].map((item) => <div key={item} className="hk-panel rounded-3xl p-6"><h2 className="hk-gold-text text-xl font-semibold">{item}</h2><p className="mt-3 text-muted-foreground">The system stays calm, minimal, and focused on usable insight.</p></div>)}</section><CtaBand /></>; }
