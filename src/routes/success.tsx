import { createFileRoute, Link } from "@tanstack/react-router";
export const Route = createFileRoute("/success")({ head: () => ({ meta: [{ title: "Success — Hiren Kundli" }, { name: "description", content: "Your Hiren Kundli booking step was completed." }] }), component: () => <Status title="Booking step completed" body="Your details have been received. The next instruction will be shared on WhatsApp." /> });
function Status({ title, body }: { title: string; body: string }) { return <section className="hk-container py-20"><div className="hk-panel rounded-3xl p-8 text-center"><p className="hk-eyebrow">Success</p><h1 className="hk-heading mt-4 text-5xl">{title}</h1><p className="mx-auto mt-5 max-w-xl text-muted-foreground">{body}</p><Link to="/sessions" className="hk-button-primary mt-7 inline-flex rounded-full px-6 py-3 font-semibold">Back to Sessions</Link></div></section>; }
